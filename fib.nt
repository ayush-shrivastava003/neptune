fn fib(n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 2) + fib(n - 1);
}

let start = time();
for (let i = 0; i < 20; i = i + 1;) print(fib(i));
let end = time();
print("Total time:");
print(end-start);